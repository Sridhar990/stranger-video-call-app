async def mock_send_verification_email(
    email: str,
    username: str,
    verification_link: str,
):
    return None

async def mock_send_password_reset_email(
    email: str,
    username: str,
    reset_link: str,
):
    return None